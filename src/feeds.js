/** an array with the feeds... **/
//TODO.. this is manually updated is there anywhere we can get feeds from?

var feeds = [
    {
        hostname: 'www.startuppodden.se',
        port: 80,
        path: '/feed/',
        method: 'GET'
    }];

module.exports = feeds;